# Salesforce DX
.sf/
.sfdx/
.localdevserver/
.deploy-tmp/

# LWC
**/lwc/**/*.css.map
**/lwc/**/node_modules/

# IDE
.vscode/
.cursor/
.idea/
.agents/

# Claude: ignore everything EXCEPT the shipped installer skill. The skill makes
# a fresh clone self-contained (Claude auto-discovers it). settings.local.json
# is a local permission allowlist — not secret, but local noise — keep ignored.
.claude/*
!.claude/skills/
.claude/settings.local.json

# OS
.DS_Store

# Node
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*

skills-lock.json

# Runtime artifacts created by "sf agent publish" — never committed
**/bots/**
genAiPlannerBundles/

# Local scratch/test output
/tmp/
nanobanana-output/
brochure/

# Finder copy/paste duplicates — never commit
**/*\ copy.*
**/*\ copy\ [0-9]*.*

# Secrets — never commit anything matching these
.secrets/
secrets/
.env
.env.*
!.env.example
.heroku/
*.pem
*.key
*.p12
*.jks
*.pfx

# Stashed observability seed data (demo data export)
.stash/

# Observability dataset — SHIP the dir (it's the portable fallback data-load
# path for the observability tier). Keep ignoring the regenerable 637KB zip.
skywave-observability-dataset.zip

# ECv2 reference raw assets are Salesforce "Company Confidential" minified
# files — never commit them (repo is planned to go public). We keep only our
# own DERIVED notes/values: the markdown docs + keyframes-decoded.css.
docs/ecv2-reference/raw-assets/home_view*.js
docs/ecv2-reference/raw-assets/init.min.js
docs/ecv2-reference/raw-assets/init.min.css
docs/ecv2-reference/raw-assets/embedded-messaging-styling.min.css
docs/ecv2-reference/raw-assets/iframe-shell.html
docs/ecv2-reference/raw-assets/*-extract.txt
__pycache__/
